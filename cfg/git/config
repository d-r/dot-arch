[user]
    email = "9402+d-r@users.noreply.github.com"
    name = "Daniel Rosengren"

[init]
    defaultBranch = "main"

[diff]
    # Show diff for changed submodule
	submodule = diff

[push]
    # Removes the need to do `git push — set-upstream origin <branch>` when the
    # branch does not yet exist upstream.
    autoSetupRemote = true

[alias]
    # Status
    st = "status --short"
    lg = "log --oneline --decorate"
    lf = "log --name-status"
    last = "log -1 HEAD --stat"

    # Changes
    ca = "commit --all --amend --no-edit"
    amend = "commit --amend"
    nm = "!git reset --hard HEAD && git clean -d --force"
    undo = "reset --hard HEAD~"
    discard = "checkout --"
    unstage = "reset head --"
    untrack = "rm -r --cached"
    pf = "push --force"

    # Branches
    co = "checkout"
    nb = "checkout -b"
    db = "branch -D"
    drb = "push origin --delete"
    mb = "branch --force" # move
    rb = "branch --move" # rename

    # Lists
    lsb = "branch --all -vv"
    lst = "tag list"
    lsr = "remote -v"
    lss = "stash list"

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
