#!/usr/bin/env nu

use ~/dot/nu/kit.nu *
use ~/dot/nu/niri.nu *

# Open the given URL in Chromium's app mode
def main [$app_id, $url] {
    wm spawn-or-focus $app_id chromium --class=($app_id) --app=($url)
}
