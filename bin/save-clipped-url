#!/usr/bin/env nu

use kit *

let $s = (wl-paste)
if ($s | is-url) {
    tui-term save-url $s
} else if ($s | is-empty) {
    notify-send "Clipboard is empty"
} else {
    notify-send "Clipboard contents is not a URL"
}
