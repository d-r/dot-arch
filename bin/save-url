#!/usr/bin/env nu

use dot *

def main [$url] {
    let $links = (open $env.DOT_LINK_FILE)
    let $key = ($links | key-of $url | default "")

    let $key = (gum input --placeholder $"Key for ($url)" --value $key)

    if ($key | is-not-empty) {
        (links | upsert $key $url) | to toml | save -f $env.DOT_LINK_FILE
        notify-send $'Bookmark "($key)" saved'
    }
}
