#!/usr/bin/env nu

const $CONFIG = ("~/.config/kitty/tui.conf" | path expand)

kitty --config $CONFIG --app-id tui.visit tui.qb-visit

let $url = (open /tmp/qb-url)
echo $"open ($url)" o> $env.QUTE_FIFO
