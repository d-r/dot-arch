#-------------------------------------------------------------------------------
# VARIABLES

$run_menu = fuzzel

$term_class = kitty
$term = kitty

$scratch_term_class = sc-term
$scratch_term = $term --class $scratch_term_class

#-------------------------------------------------------------------------------
# ENVIRONMENT VARIABLES

# https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

#-------------------------------------------------------------------------------
# THEMING / DARK MODE

# GTK4
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"

# GTK3
exec = gsettings set org.gnome.desktop.interface gtk-theme "adw-gtk3-dark"
# TODO: Is this the correct name? How would I find that out?
exec = gsettings set org.gnome.desktop.interface icon-theme "adw-gtk3"

# GTK3
# env = GTK_THEME,Adwaita:dark

#-------------------------------------------------------------------------------
# MONITORS

# https://wiki.hyprland.org/Configuring/Monitors/

# name, resolution, position, scale
monitor=,preferred,auto,1.0

#-------------------------------------------------------------------------------
# WORKSPACES

# https://wiki.hyprland.org/Configuring/Workspace-Rules/

$r1_1 = 1
$r1_2 = 2
$r1_3 = 3
$r1_4 = 4

$r2_1 = 5
$r2_2 = 6
$r2_3 = 7
$r2_4 = 8

$r3_1 = 9
$r3_2 = 10
$r3_3 = 11
$r3_4 = 12

# Room 1
workspace = $r1_1, defaultName:1_Q, persistent:true
workspace = $r1_2, defaultName:2_Q, persistent:true
workspace = $r1_3, defaultName:3_Q, persistent:true
workspace = $r1_4, defaultName:4_Q, persistent:true

# Room 2
workspace = $r2_1, defaultName:1_W, persistent:true
workspace = $r2_2, defaultName:2_W, persistent:true
workspace = $r2_3, defaultName:3_W, persistent:true
workspace = $r2_4, defaultName:4_W, persistent:true

# Room 3
workspace = $r3_1, defaultName:1_E, persistent:true
workspace = $r3_2, defaultName:2_E, persistent:true
workspace = $r3_3, defaultName:3_E, persistent:true
workspace = $r3_4, defaultName:4_E, persistent:true

# Terminal scratchpad
workspace = special:term, gapsout:30, on-created-empty:$scratch_term -d ~/dot
windowrule = group set always, class:$scratch_term_class

# Music player scratchpad
workspace = special:music, gapsout:30, on-created-empty:$term rmpc

# Sound settings scratchpad
workspace = special:snd, on-created-empty:pavucontrol
windowrule = float, class:.*pavucontrol
windowrule = size 900 700, class:.*pavucontrol
windowrule = center, class:.*pavucontrol

#-------------------------------------------------------------------------------
# WINDOWS

# https://wiki.hyprland.org/Configuring/Window-Rules/

# Open browser as a group
windowrule = group set always, class:firefox
windowrule = group set always, class:Brave-browser
windowrule = group set always, class:.*.qutebrowser

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland.
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

#-------------------------------------------------------------------------------
# STARTUP - INFRASTRUCTURE

# https://wiki.hyprland.org/Configuring/Keywords/

# Start notification daemon
exec-once = swaync

# Set wallpaper
exec-once = swaybg --image ~/images/wallpapers/alena-aenami-all-i-need-1080p.jpg

# Enable top bar
exec-once = waybar

# Start udiskie daemon to enable automounting of external drive
exec-once = udiskie

#-------------------------------------------------------------------------------
# STARTUP - APPLICATIONS

# The [workspace X] form doesn't work for some apps. Presumably X11 ones?
# You'd think Code would fully support Wayland out of the box by now, but that
# does not seem to be the case?
# TODO: Stop using Code.

# Room 1 (Lab)

exec-once = [workspace $r1_1 silent] qutebrowser

exec-once = code
windowrule = workspace $r1_2 silent, class:Code

# Room 2 (Studio)

exec-once = renoise
windowrule = workspace $r2_1 silent, class:Renoise

# Room 3 (Study)

exec-once = [workspace $r3_1 silent] firefox
exec-once = [workspace $r3_2 silent] obsidian
